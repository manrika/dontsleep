<div class="modal fade" id="editReleaseModal" tabindex="-1" aria-labelledby="editReleaseModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="editReleaseModalLabel">Edit artist</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= simple_form_for release, url: update_release_path(release) do |f| %>
          <%= f.input :track_title  %>
          <%= f.input :artist_name %>
          <%= f.input :spotify_link  %>
          <%= f.input :spotify_embed  %>
          <%= f.input :apple  %>
          <%= f.input :tidal  %>
          <%= f.input :amazon  %>
          <%= f.input :photo, class: "btn btn-primary", as: :file %>
      </div>
      <div class="modal-footer">
          <button type="button" class="btn btn-outline-secondary" data-dismiss="modal">CANCEL</button>
          <%= f.submit value: "SAVE", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
