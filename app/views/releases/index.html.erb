<%= render 'shared/navbar' %>

<div class="page-layout">
  <h1 style="color: rgba(242, 86, 101, 0.79)">Releases</h1>

  <% if @releases.count > 1 %>
    <%= render 'carousel' %>
  <% else %>
    <%= render 'single_release' %>
  <% end %>
</div>
